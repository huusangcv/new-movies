const routes = {
  home: '/',
  single: '/movies/single',
  series: '/movies/series',
  new: '/movies/new',
  search: '/search',
  developing: '/developing',
  detail: '/movie/:slug',
  watch: '/watch/:slug',
  browse: '/browse',
  notfound: '*',
};

export default routes;
